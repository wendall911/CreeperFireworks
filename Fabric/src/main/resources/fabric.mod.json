{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${fabric_mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "issues": "${issue_tracker}"
  },
  "license": "${license}",
  "icon": "${mod_id}_icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "creeperfireworks.CreeperFireworksFabric"
    ],
    "client": [
      "creeperfireworks.CreeperFireworksFabricClient"
    ],
    "spectrelib-config": [
      "creeperfireworks.FabricConfigInitializer"
    ]
  },
  "mixins": [
    "creeperfireworks.mixins.json"
  ],
  "depends": {
    "fabricloader": "${fabric_loader_min_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}",
    "java": ">=${java_version}"
  }
}
